# macOS

The macOS platform directory is reserved now so Harr's repository layout is stable before a macOS installer is implemented. Shared policy, skills and host config writers already live under `../common`; future macOS work should add only launchd/install/path glue and a platform-specific LeanCTX wrapper/config where needed.

macOS installation is not implemented in this PR.
